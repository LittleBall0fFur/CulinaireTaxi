@{
    if (UserAgent.IsAuthenticated)
    {
        if (UserAgent.Account.AccountType != AccountType.CUSTOMER) {
            Context.Response.Redirect("/App/CompanyPage");
        } else
        {
            Context.Response.Redirect("/App/CustomerPage");
        }
    }
}
